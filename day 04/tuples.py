# You can make only take info from tuples, not edit them
numbers = (1,2,3)
print(numbers[0])
# numbers[0] = 10 THIS GENERATES AN ERROR!!
print(numbers[0])
#numbers.count()
#numbers.index()

#If you dont want to modify a list, use a tuple